<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('new_homepage') }}">{% trans %} Main Menu {% endtrans %}</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        {% if is_granted('ROLE_ADMIN') %}
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="users" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              {% trans %} Users {% endtrans %}
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <li><a class="dropdown-item" href="{{ path('new_user_index') }}">{% trans %} List Users {% endtrans %}</a></li>
              <li><a class="dropdown-item" href="{{ path('new_user_add') }}">{% trans %} Add User {% endtrans %}</a></li>
            </ul>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="tasks" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              {% trans %} Tasks {% endtrans %}
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <li><a class="dropdown-item" href="{{ path('new_task_index') }}">{% trans %} List Tasks {% endtrans %}</a></li>
              <li><a class="dropdown-item" href="{{ path('new_task_add') }}">{% trans %} Add Task {% endtrans %}</a></li>
            </ul>
          </li>
        {% endif %}
        {% if is_granted('ROLE_USER') %}
          <li>
            <a href="{{ path('new_task_custom') }}" class="dropdown-item">{% trans %} My tasks {% endtrans %}</a>
          </li>
        {% endif %}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <a href="{{ path('new_logout') }}" class="dropdown-item">{% trans %} Logout {% endtrans %}</a>
        </li>
      </ul>
    </div>
  </div>
</nav>